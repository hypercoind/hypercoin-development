---
interface Props {
  headline?: string;
  tagline?: string;
  githubUrl?: string;
}

const {
  headline = "Bitcoin Research and FOSS Development",
  tagline = "Building open-source tools with emphasis on the Bitcoin ecosystem.",
  githubUrl = "https://github.com/hypercoind",
} = Astro.props;
---

<section class="hero">
  <div class="container">
    <h1>{headline}</h1>
    <p>{tagline}</p>
    <div class="cta-buttons">
      <a href={githubUrl} class="btn btn-secondary" target="_blank" rel="noopener noreferrer">
        <span>GitHub</span>
        <span>↗</span>
      </a>
    </div>
  </div>
</section>
